import express from "express";
const router = express.Router();
// route -> /api/v1/dashboard/stats
// router.post("/stats", adminMiddleware, getDashboardStats);
// route -> /api/v1/dashboard/pie
// router.post("/pie", adminMiddleware, getPieChart);
// route -> /api/v1/dashboard/stats
// router.post("/bar", adminMiddleware, getBarChart);
// route -> /api/v1/dashboard/stats
// router.post("/line", adminMiddleware, getLineChart);
export default router;
